# Photos

Put wedding photos in this folder, then reference them in `data/gallery.ts`
as `src: "/images/your-file.jpg"` (with the correct width/height ratio).

These images feed both the home-page slideshow and the `/gallery` page.
Portrait shots (~4:5) look best in the slideshow.
